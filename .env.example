# Variables de Supabase — copia este archivo como .env.local y completa los valores.
# Sin estas variables la app corre en modo local (Zustand + localStorage).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
